# Archive

This directory contains modules intentionally removed from the active OSS surface.

- `dashboard/`: archived product-style UI app.

Archived modules are kept for reference only. They are not part of default quickstarts, compose profiles, or required CI checks.
